<Alloy>
  <!-- Main Window -->
  <Window id="vlothieHome" title="Todo" platform="android,ios">

    <!-- header -->
    <View id="header">
      <Label id="title">Vlothie</Label>
    </View>

    <!-- Main table for tasks
      Use model-view binding to render the rows fo this table
      based on the models in the given collection. "dataCollection"
      represents the collection used to populate this table.
      "dataWhere" is a function used to filter the list of
      models processed within the collection. "dataTransform"
      is a function that will be executed on each model as it
      is rendered as a row. It gives the opportunity to modify
      the model's values before they are applied to each row.
    -->
    <View id="homeContent">
        
	    <View id="hello">
	      <Label class="sectionTitle">Good morning! 16 c / 72 f be beautiful</Label>
	    </View>
	   
	    <View id="yourLooks">
	    	<View class="yourLooksEach">
	     	 		<Label class="sectionTitle">1 - The day </Label>
	       </View>
	   	
	   		<View class="yourLooksEach yourLooksEachRow1">
	     	   		 <Label class="sectionTitle">2 - That meeting </Label> 
		    </View>
	   	
	   		<View class="yourLooksEach yourLooksEachRow2">
	     	    	 <Label class="sectionTitle">3 - Sporty you!</Label>
	  		</View>
	    </View>
	
		<View id="trends">
	      <Label class="sectionTitle">be in!</Label>
	   </View> 

	
	</View>
	
	<Require src="menu"/>
  
  </Window>

  <!-- Main Window for unsupported platforms -->
  
</Alloy>